---
sidebar_position: 2
title: 폴더 구조
---

import FileTree from '@site/src/components/FileTree';

# 폴더 구조

## 저장소 전체

<FileTree nodes={[
  { name: 'magFlip/', children: [
    { name: 'CLAUDE.md', note: 'Claude Code용 프로젝트 가이드 (패키지별 CLAUDE.md 도 있음)', highlight: true },
    { name: '.claude/skills/', note: '저장소 공통 skill: local-dev, release' },
    { name: 'package.json', note: 'workspaces 루트 · build:local / test / serve / docs:* 스크립트', highlight: true },
    { name: 'scripts/', children: [
      { name: 'serve.mjs', note: '의존성 없는 로컬 정적 서버 (npm run serve)' },
    ]},
    { name: 'packages/', note: 'npm 으로 배포되는 라이브러리들', highlight: true, children: [
      { name: 'core/', note: '@magflip/core — 모델, 책장, 뷰어, 공통 유틸' },
      { name: 'flipview/', note: '@magflip/flipview — Flip 효과 뷰 플러그인' },
      { name: 'scrollview/', note: '@magflip/scrollview — 스크롤 뷰 플러그인 (개발 중)' },
      { name: 'minjs/', note: '@magflip/minjs — 브라우저용 단일 번들' },
    ]},
    { name: 'docs/', children: [
      { name: 'examples/', children: [
        { name: 'local/', note: '로컬 빌드 번들로 동작하는 테스트 페이지', highlight: true },
        { name: 'prebuild/', note: 'CDN 번들 예제 (GitHub Pages 공개 데모) + 샘플 이미지' },
        { name: 'react-ts/', note: 'React + Vite 예제 앱' },
      ]},
    ]},
    { name: 'website/', note: 'Docusaurus 문서 사이트 (별도 npm 프로젝트)', highlight: true, children: [
      { name: 'user-docs/', note: '사용자 가이드 → /user' },
      { name: 'dev-docs/', note: '개발자 가이드 → /dev' },
      { name: 'src/', note: '홈 페이지 · FileTree / DemoFrame 컴포넌트 · theme/Mermaid wrapper' },
      { name: 'static/img/', note: '이미지 (flip-math: 기하 설명 그림)' },
      { name: 'scripts/sync-demo.mjs', note: '로컬 번들을 static/demo 로 복사' },
    ]},
  ]},
]} />

## 패키지 내부 공통 구조

모든 TypeScript 패키지는 같은 모양을 가집니다.

<FileTree nodes={[
  { name: 'packages/<name>/', children: [
    { name: 'src/', note: '✏️ 소스 코드 — 여기만 수정합니다', highlight: true, children: [
      { name: 'index.ts', note: '공개 API 진입점 (export * + CSS import)' },
    ]},
    { name: 'CLAUDE.md', note: '패키지별 작업 가이드' },
    { name: '.claude/skills/', note: '패키지 전용 skill' },
    { name: 'index.js', note: '🔨 빌드 결과 (ESM) · git ignore' },
    { name: 'index.d.ts', note: '🔨 빌드 결과 (타입) · git ignore' },
    { name: 'index.css', note: '🔨 빌드 결과 (core 만 추출) · git ignore' },
    { name: 'dist/', note: '🔨 tsc 중간 결과 · git ignore' },
    { name: 'rollup.config.js', note: 'index.d.ts / index.js 두 개의 번들 설정' },
    { name: 'tsconfig.json' },
    { name: 'package.json', note: 'build / build:local / typecheck 스크립트' },
  ]},
]} />

## packages/core

<FileTree nodes={[
  { name: 'src/', children: [
    { name: 'common/', note: 'DOM·상태에 의존하지 않는 공통 코드', children: [
      { name: 'models.ts', note: '인터페이스와 enum (IBookData, IPageData, IBookView, Zone, EventStatus …)', highlight: true },
      { name: 'dimension.ts', note: 'ISize, SizeExt(대각선 포함), BookSize' },
      { name: 'shape.ts', note: 'Point, Line, Rect (코너/중앙 좌표 getter)' },
      { name: 'mzMath.ts', note: '각도·거리·수선의 발·요소 위치 계산' },
      { name: 'event.ts', note: 'MZEvent — 간단한 이벤트 emitter' },
      { name: 'helper.ts', note: 'deepMerge' },
    ]},
    { name: 'core/', note: '런타임 객체', children: [
      { name: 'base.ts', note: 'Base extends MZEvent' },
      { name: 'bookShelfManager.ts', note: '책장 ↔ 뷰어 조율 (진입점)', highlight: true },
      { name: 'bookShelf.ts', note: '#bookShelf DOM 과 책 목록' },
      { name: 'bookViewer.ts', note: '#bookViewer DOM, 뷰 등록/선택, 줌, 닫기', highlight: true },
      { name: 'book.ts / bookEl.ts', note: '책 데이터 / 책 DOM' },
      { name: 'page.ts / pageEl.ts', note: '페이지 데이터 / 페이지 DOM' },
      { name: 'pageLabel.ts / pageLabelEl.ts', note: '라벨 데이터 / 라벨 DOM' },
      { name: 'core.css', note: '책장·뷰어 기본 스타일' },
    ]},
    { name: 'index.ts' },
  ]},
]} />

## packages/flipview

<FileTree nodes={[
  { name: 'src/', children: [
    { name: 'flipView.ts', note: 'IBookView 구현 · DOM/SVG 생성 · 마우스 이벤트 · 그림자 렌더링 · 페이지 이동', highlight: true },
    { name: 'flipManager.ts', note: 'class Flipping — 이벤트 상태, 애니메이션, flip() 기하 계산', highlight: true },
    { name: 'pageWindow.ts', note: '6칸 페이지 윈도우 (moveLeft / moveRight)' },
    { name: 'flipData.ts', note: 'flip() 계산 결과 (page2 위치/회전, mask 좌표, shadow)' },
    { name: 'flipDiagonals.ts / flipDiagonal.ts', note: '마우스 제한 영역(Area 1~4) 계산' },
    { name: 'flipActionLine.ts', note: '드래그 기준 수평선 (left/center/right 점)' },
    { name: 'gutter.ts', note: '책 가운데 접히는 선 (Rect)' },
    { name: 'flipView.css', note: 'nth-child 기반 페이지 배치, z-index, 그림자, zone 크기' },
  ]},
  { name: '__tests__/', note: 'Jest 단위 테스트 (core 소스를 직접 import)' },
  { name: 'jest.config.cjs' },
]} />

## "무엇을 고치려면 어디로?"

| 하고 싶은 일 | 파일 |
| --- | --- |
| 책/페이지 데이터에 필드 추가 | `core/src/common/models.ts` → `core/src/core/book.ts` 또는 `page.ts` |
| 페이지 DOM 구조 변경 | `core/src/core/pageEl.ts` (+ `flipview/src/flipView.css`) |
| 책장 UI 변경 | `core/src/core/bookShelf.ts`, `core.css` |
| 넘김 애니메이션 속도/곡선 | `flipview/src/flipManager.ts` (`animateFlip`, `animateReadyToFlip`), `flipView.ts` (`autoFlip`) |
| 접힘 모양 계산 | `flipview/src/flipManager.ts` `flip()` |
| 그림자 | `flipview/src/flipView.ts` `setShadow1/3/5/6`, `flipView.css` |
| 마우스 반응 영역 크기 | `flipview/src/flipView.css` `#mzZone*` |
| 새 뷰 플러그인 | `packages/scrollview` 를 복사해서 시작 |
| 번들 구성 | `packages/minjs/src/index.js`, `rollup.config.js` |
